
const USERREGISTRY_API_ENDPOINT = import.meta.env.VITE_USERREGISTRY_API_ENDPOINT;

function Register() {

    // register form with email, password, firstname, lastname and confirm password fields and submit button

    // userRegistry API call to /register endpoint with email and password

    // on success, store token in localStorage and redirect to home page / product list

    return (
        <>


        </>
    )
}

export default Register;